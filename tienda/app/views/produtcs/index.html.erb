<h1>Listing produtcs</h1>

<table>
  <tr>
    <th>Codigo</th>
    <th>Descripcion</th>
    <th>Precio</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @produtcs.each do |produtc| %>
  <tr>
    <td><%= produtc.codigo %></td>
    <td><%= produtc.descripcion %></td>
    <td><%= produtc.precio %></td>
    <td><%= link_to 'Show', produtc %></td>
    <td><%= link_to 'Edit', edit_produtc_path(produtc) %></td>
    <td><%= link_to 'Destroy', produtc, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Produtc', new_produtc_path %>
